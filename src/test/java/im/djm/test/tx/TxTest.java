package im.djm.test.tx;

import org.junit.Test;

public class TxTest {

	@Test
	public void newTx() {
		// Tx tx = new Tx();
		//
		// assertThat(tx).isNotNull();
		// assertThat(tx.isCoinbase()).isFalse();
		// assertThat(tx.getInputSize()).isEqualTo(0);
		// assertThat(tx.getOutputSize()).isEqualTo(0);
	}

}
